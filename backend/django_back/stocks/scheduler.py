from apscheduler.schedulers.background import BackgroundScheduler
import pytz
# from .tasks import collect_minute_data  # collect_minute_data 함수 임포트

# def start():
#     scheduler = BackgroundScheduler(timezone=pytz.timezone("Asia/Seoul"))
#     scheduler.add_job(collect_minute_data, 'interval', minutes=30)
#     scheduler.start()
